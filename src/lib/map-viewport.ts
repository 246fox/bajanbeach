/**
 * Map "desktop" layout: wide enough for Tailwind `sm`-style behaviour *and* tall enough
 * that planted offshore tiles fit on the map. Used for planted offshore tiles, beach
 * InfoWindow vs bottom sheet, and the below-map offshore row.
 */
export const MAP_VIEWPORT_DESKTOP_MQ = "(min-width: 640px) and (min-height: 600px)";
