/**
 * Tailwind `sm` breakpoint — single source for map desktop vs mobile (planted offshore
 * tiles, beach InfoWindow vs bottom sheet, below-map offshore row).
 */
export const MAP_VIEWPORT_DESKTOP_MQ = "(min-width: 640px) and (min-height: 600px)";

/** Minimum map container width (px) before planted offshore tiles are shown. */
export const OFFSHORE_PLANTED_MIN_WIDTH_PX = 875;

/** Minimum map container height (px) before planted offshore tiles are shown. */
export const OFFSHORE_PLANTED_MIN_HEIGHT_PX = 500;
