/**
 * Tailwind `sm` breakpoint — single source for map desktop vs mobile (planted offshore
 * tiles, beach InfoWindow vs bottom sheet, below-map offshore row).
 */
export const MAP_VIEWPORT_DESKTOP_MQ = "(min-width: 640px)";
